[project]
name = "owasp-benchmarkpython-scanners"
version = "0.1.0"
description = "Neutral, reproducible scanner comparison on OWASP BenchmarkPython"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }

[tool.ruff]
line-length = 100
